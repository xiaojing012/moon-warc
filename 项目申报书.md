# moon-warc 项目申报书

## 基本信息

- 项目名称：moon-warc —— MoonBit 的 WARC 1.1 流式解析、校验、索引与审计工具库
- 参赛者：宋晓静
- 联系方式：2669550012@qq.com
- GitHub 仓库地址：https://github.com/xiaojing012/moon-warc
- 项目方向：MoonBit 数字保存／Web 归档基础库
- 项目类型：原创项目

## 项目简介

moon-warc 是一个面向 MoonBit 生态的 WARC 1.1（ISO 28500:2017）流式处理工具库，围绕 Web 归档容器格式提供记录骨架解析、流式解码、确定性写入、语义校验、分段支持、内存索引与审计能力。WARC 是互联网档案馆、国家图书馆与 Common Crawl 等机构存储网络采集资源的标准格式；本项目在纯 MoonBit 中实现其记录定界与语义层，块内容为任意二进制并以 Content-Length 唯一定界（绝不扫描块内版本行），适用于爬虫存储层、归档数据离线分析、格式迁移工具与归档质检流水线。

## 项目方向与适用场景

项目属于 MoonBit 数字保存与 Web 归档基础设施方向，适用于网络爬虫归档写入、Web 档案馆藏解析与质检、大规模归档统计分析、采集完整性审计等场景。通过将定界解析、语义校验与业务分析分层，降低上层应用重复实现 WARC 语义时产生边界错误的风险，并为 MoonBit 生态提供归档处理的可复用基础组件。

## 实现的核心功能

- 解析版本行与有序命名字段（字段名不区分大小写、原始拼写保留）；
- Content-Length 强制字段与二进制安全定界：溢出安全十进制解析、重复检测、块长度校验，解析器绝不在块内搜索 "WARC/1.1"；
- 八种标准记录类型与强制字段访问器（Record-ID、Target-URI、Date、Type 等）；
- 整档缓冲解析（含尾随垃圾诊断）与流式 WarcDecoder（feed/finish，结果与分块无关）；
- 确定性写入器与程序化构建器（构建期拒绝非法字段名与含 CR/LF 的值）；
- 语义校验器：字段放置规则、值域校验（IPv4/IPv6、W3CDTF 日期、摘要语法、URI 形态）、重复字段与档案级记录 ID 查重；
- 分段字段解析与档案级分段组校验（组连续、编号递增、Total-Length 仅组末）；
- 内存索引（按记录 ID／目标 URI／类型查询）与档案统计（记录数、块字节、类型直方图、日期范围）；
- 审计引擎（Info/Warning 分级发现：未知类型、空块、缺摘要、悬空引用等）；
- 结构化错误（阶段／种类／偏移／消息）、资源限制（记录数／块大小／字段数）、208 个测试覆盖 wasm-gc、js、native 三目标、截断点等价属性测试、CLI 工具、五个示例程序与 CI 验证。

## 当前实现状态

截至 2026 年 8 月 15 日，仓库共 13 次真实连续提交（每次提交均通过三目标全量测试后推送，无空提交、无改写历史）：

1. 初始化 WARC 核心模型与安全解析基础（错误模型、资源限制、溢出安全十进制解析）；
2. 解析版本行与有序命名字段；
3. 解析 Content-Length 并实现二进制安全记录定界；
4. 记录类型与强制字段辅助函数；
5. 完整 WARC 档案缓冲解析（含尾随垃圾诊断）；
6. 流式 WarcDecoder（结果与喂入分块无关）；
7. 记录写入器与构建器（往返测试）；
8. 语义校验器与摘要语法解析；
9. 分段解析与档案级校验（分段组规则、记录 ID 查重）；
10. 档案索引、查询与聚合统计；
11. 审计引擎与截断压力测试（全截断点双向等价）；
12. CLI 工具、示例程序与 CI 验证（verify_all.ps1 一键全量验证）；
13. 完整文档（架构、安全、测试、复现、规范映射收尾）。

测试矩阵：208 个测试在 wasm-gc、js、native 三个编译目标全部通过；源码约 3000 行、测试约 2800 行。

## 原创或参考说明

本项目为原创项目，不是对现有开源代码库的移植，也未复制第三方项目源码。协议行为依据 ISO 28500:2017（WARC 1.1，IIPC 公开维护版）各条款独立设计与实现，规范映射见 docs/specification-map.md。项目采用 Apache License 2.0 许可证，运行时仅依赖 MoonBit core（buffer、debug、encoding/utf8、env），见 THIRD_PARTY_NOTICES.md。
